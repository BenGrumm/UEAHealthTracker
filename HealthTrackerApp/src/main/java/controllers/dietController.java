package controllers;
import javafx.fxml.FXML;
import javafx.fxml.Initializable;

import java.net.URL;
import java.util.ResourceBundle;

public class dietController extends Controller implements Initializable {
    @Override
    public void initialize(URL url, ResourceBundle resourceBundle) {

    }

    /*
    to do:
    initialize function
    Connect fxml to controller
    be able to print entered data to console when save button pressed
    validate
    save to database when save button pressed
     */
}
